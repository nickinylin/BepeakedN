<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:colorBackground">@color/highlighted_text_material_light</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- This is all for styling alertDialog // Nicki -->

    <style name="NumberPickerTextColorStyle">
        <item name="android:textColorPrimary">#f51616</item>
    </style>

    <!--<style name="MyTitleTextStyle">-->
        <!--<item name="android:textColor">#FFEB3B</item>-->
        <!--<item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>-->
    <!--</style>-->

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">#FFC107</item>

        <!--<item name="android:windowTitleStyle">@style/MyTitleTextStyle</item>-->
        <!-- Used for the title and text -->
        <!--<item name="android:textColorPrimary">#FFFFFF</item>-->
        <!-- Used for the background -->
    <!--<item name="android:background">#FFFFFF</item>-->
    </style>

<!-- alertDialog styling end // Nicki -->

</resources>
